<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Style x:Key="FluentScrollBarPageButtonStyle"
           TargetType="{x:Type RepeatButton}">
		<Setter Property="OverridesDefaultStyle" Value="True" />
		<Setter Property="Focusable" Value="False" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="Background" Value="{DynamicResource Brush.Transparent}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type RepeatButton}">
					<Border Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True" />
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="FluentScrollBarThumbStyle"
           TargetType="{x:Type Thumb}">
		<Setter Property="OverridesDefaultStyle" Value="True" />
		<Setter Property="Focusable" Value="False" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="Background" Value="{DynamicResource ScrollBarThumbBrush}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Thumb}">
					<Border x:Name="ThumbChrome"
                            Margin="2,0,2,0"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{DynamicResource Inset.None}"
                            CornerRadius="{DynamicResource Radius.None}"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True" />

					<ControlTemplate.Triggers>
						<DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}"
                                     Value="Horizontal">
							<Setter TargetName="ThumbChrome"
                                    Property="Margin"
                                    Value="0,2,0,2" />
						</DataTrigger>

						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}"
                                           Value="Vertical" />
								<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}"
                                           Value="True" />
							</MultiDataTrigger.Conditions>
							<Setter TargetName="ThumbChrome"
                                    Property="Margin"
                                    Value="1,0,1,0" />
							<Setter TargetName="ThumbChrome"
                                    Property="Background"
                                    Value="{DynamicResource ScrollBarThumbHoverBrush}" />
						</MultiDataTrigger>

						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}"
                                           Value="Horizontal" />
								<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}"
                                           Value="True" />
							</MultiDataTrigger.Conditions>
							<Setter TargetName="ThumbChrome"
                                    Property="Margin"
                                    Value="0,1,0,1" />
							<Setter TargetName="ThumbChrome"
                                    Property="Background"
                                    Value="{DynamicResource ScrollBarThumbHoverBrush}" />
						</MultiDataTrigger>

						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="ThumbChrome"
                                    Property="Background"
                                    Value="{DynamicResource ScrollBarThumbHoverBrush}" />
						</Trigger>

						<Trigger Property="IsDragging" Value="True">
							<Setter TargetName="ThumbChrome"
                                    Property="Background"
                                    Value="{DynamicResource ScrollBarThumbPressedBrush}" />
						</Trigger>

						<Trigger Property="IsEnabled" Value="False">
							<Setter TargetName="ThumbChrome"
                                    Property="Opacity"
                                    Value="{DynamicResource Opacity.Disabled.Control}" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="FluentScrollBarStyle"
           TargetType="{x:Type ScrollBar}">
		<Setter Property="OverridesDefaultStyle" Value="True" />
		<Setter Property="Focusable" Value="False" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="Background" Value="{DynamicResource Brush.Transparent}" />
		<Setter Property="BorderThickness" Value="{DynamicResource Inset.None}" />
		<Setter Property="SnapsToDevicePixels" Value="True" />
		<Setter Property="UseLayoutRounding" Value="True" />
		<Setter Property="Width" Value="{DynamicResource Space.12}" />
		<Setter Property="MinWidth" Value="{DynamicResource Space.12}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ScrollBar}">
					<Grid x:Name="Root"
                          Background="{TemplateBinding Background}"
                          SnapsToDevicePixels="True"
                          UseLayoutRounding="True">
						<Border x:Name="TrackChrome"
                                Margin="{DynamicResource Inset.ScrollBarTrack.Vertical}"
                                Background="{DynamicResource ScrollBarTrackBrush}"
                                BorderThickness="{DynamicResource Inset.None}"
                                CornerRadius="{DynamicResource Radius.None}"
                                Opacity="0.42"
                                SnapsToDevicePixels="True"
                                UseLayoutRounding="True" />

						<Track x:Name="PART_Track"
                               Orientation="{TemplateBinding Orientation}"
                               IsDirectionReversed="True">
							<Track.DecreaseRepeatButton>
								<RepeatButton x:Name="DecreaseButton"
                                              Style="{StaticResource FluentScrollBarPageButtonStyle}"
                                              Command="{x:Static ScrollBar.PageUpCommand}" />
							</Track.DecreaseRepeatButton>

							<Track.Thumb>
								<Thumb x:Name="ScrollThumb"
                                       Style="{StaticResource FluentScrollBarThumbStyle}"
                                       MinHeight="{DynamicResource Space.36}" />
							</Track.Thumb>

							<Track.IncreaseRepeatButton>
								<RepeatButton x:Name="IncreaseButton"
                                              Style="{StaticResource FluentScrollBarPageButtonStyle}"
                                              Command="{x:Static ScrollBar.PageDownCommand}" />
							</Track.IncreaseRepeatButton>
						</Track>
					</Grid>

					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="TrackChrome"
                                    Property="Opacity"
                                    Value="0.9" />
						</Trigger>

						<Trigger Property="Orientation" Value="Horizontal">
							<Setter Property="Width" Value="Auto" />
							<Setter Property="MinWidth" Value="{DynamicResource Space.36}" />
							<Setter Property="Height" Value="{DynamicResource Space.12}" />
							<Setter Property="MinHeight" Value="{DynamicResource Space.12}" />
							<Setter TargetName="PART_Track"
                                    Property="IsDirectionReversed"
                                    Value="False" />
							<Setter TargetName="TrackChrome"
                                    Property="Margin"
                                    Value="{DynamicResource Inset.ScrollBarTrack.Horizontal}" />
							<Setter TargetName="ScrollThumb"
                                    Property="MinHeight"
                                    Value="{DynamicResource Space.0}" />
							<Setter TargetName="ScrollThumb"
                                    Property="MinWidth"
                                    Value="{DynamicResource Space.36}" />
							<Setter TargetName="DecreaseButton"
                                    Property="Command"
                                    Value="{x:Static ScrollBar.PageLeftCommand}" />
							<Setter TargetName="IncreaseButton"
                                    Property="Command"
                                    Value="{x:Static ScrollBar.PageRightCommand}" />
						</Trigger>

						<Trigger Property="IsEnabled" Value="False">
							<Setter TargetName="Root"
                                    Property="Opacity"
                                    Value="{DynamicResource Opacity.Disabled.Control}" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="{x:Type ScrollBar}"
           BasedOn="{StaticResource FluentScrollBarStyle}" />

</ResourceDictionary>