<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Style/Queue/QItemBase.xaml" />
        <ResourceDictionary Source="/Resources/Style/Queue/QWShared.xaml" />
        <ResourceDictionary Source="/Resources/Style/Queue/QIHeaderTpl.xaml" />
        <ResourceDictionary Source="/Resources/Style/Queue/QIStatusTpl.xaml" />
        <ResourceDictionary Source="/Resources/Style/Queue/QIProgTpl.xaml" />
        <ResourceDictionary Source="/Resources/Style/Queue/QIActionsTpl.xaml" />
        <ResourceDictionary Source="/Resources/Style/Queue/QIRedTpl.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate x:Key="FluentQueueRowTemplate">
        <Grid FlowDirection="{DynamicResource App.FlowDirection}"
              Language="{DynamicResource App.XmlLanguage}"
              SnapsToDevicePixels="True"
              UseLayoutRounding="True">
            <Border Style="{StaticResource FluentQueueTaskRowCard}">
                <Grid FlowDirection="{DynamicResource App.FlowDirection}"
                      Language="{DynamicResource App.XmlLanguage}"
                      SnapsToDevicePixels="True"
                      UseLayoutRounding="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ContentControl Grid.Row="0"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource QueueItemHeaderFieldsTemplate}"
                                    Focusable="False" />

                    <ContentControl Grid.Row="1"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource QueueItemStatusFieldsTemplate}"
                                    Focusable="False" />

                    <ContentControl Grid.Row="2"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource QueueItemProgressFieldsTemplate}"
                                    Focusable="False" />

                    <ContentControl Grid.Row="3"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource QueueItemActionsFieldsTemplate}"
                                    Focusable="False" />

                    <ContentControl Grid.Row="4"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource QueueItemRedumpFieldsTemplate}"
                                    Focusable="False" />
                </Grid>
            </Border>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
